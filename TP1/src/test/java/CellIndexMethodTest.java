import org.junit.jupiter.api.Test;

import java.io.FileNotFoundException;
import java.util.List;

import static org.junit.jupiter.api.Assertions.assertEquals;


public class CellIndexMethodTest {

//    @Test
//    void whenInsertingTwoDistancedParticles_ShouldNotBeNeighbours() throws FileNotFoundException {
//        Config config = new Config(10.0, 2, null, 0.2,
//                false, 0.1, null, null);
//
//        Area area = AreaGenerator.initializeAreaWithConfig(config);
//        area.setParticleList(List.of(new Particle(0.05, 0.05, 0), new Particle(0.25, 0.25, 0)));
//
//        CellIndexMethod cellIndexMethod = new CellIndexMethod(area);
//        cellIndexMethod.calculateNeighbours(config.isPeriodicBorderCondition());
//        assertEquals(0, cellIndexMethod.getResults().get(1L).size());
//    }

}
